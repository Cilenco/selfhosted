FROM caddy:builder AS builder

RUN xcaddy build \
--with github.com/caddy-dns/ionos \
--with github.com/Lenart12/caddy-events-store_cert

FROM caddy:latest

# Install IONOS and webdav extension
COPY --from=builder /usr/bin/caddy /usr/bin/caddy
