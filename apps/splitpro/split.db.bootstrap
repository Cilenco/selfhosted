#!/bin/bash

mkdir -p $VOLUMES_DIR/splitpro/db/data

printf $(openssl rand -hex  8) | podman secret create SPLIT_DB_USERNAME - | true
printf $(openssl rand -hex 12) | podman secret create SPLIT_DB_PASSWORD - | true