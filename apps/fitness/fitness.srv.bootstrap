#!/bin/bash

mkdir -p $VOLUMES_DIR/fitness/srv/uploads

printf $(openssl rand -hex 32) | podman secret create FITNESS_API_SECRET - | true
printf $(openssl rand -hex 32) | podman secret create FITNESS_AUTH_SECRET - | true
