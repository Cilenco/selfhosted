#!/bin/bash

mkdir -p $VOLUMES_DIR/immich/db

printf $(openssl rand -hex  8) | podman secret create IMMICH_DB_USERNAME - | true
printf $(openssl rand -hex 12) | podman secret create IMMICH_DB_PASSWORD - | true